import React from 'react'
import { Text, View } from 'react-native'
import { Movie } from '../interfaces/movie'

interface Props {
    movie: Movie
}

export const MovieItemList = ({ movie }: Props) => {
    return (
        <View>
            <Text></Text>
        </View>
    )
}